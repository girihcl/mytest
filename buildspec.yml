version: 0.1

phases:
  install:
    commands:
      - echo Install started on `date`
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
  discard-paths: yes
